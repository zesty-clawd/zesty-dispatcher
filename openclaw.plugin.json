{
  "id": "zesty-dispatcher",
  "configSchema": {
    "type": "object",
    "properties": {
      "enabled": {
        "type": "boolean",
        "default": true
      },
      "enableTool": {
        "type": "boolean",
        "default": false,
        "description": "Whether to register the manual dispatch_skills tool."
      },
      "debug": {
        "type": "boolean",
        "default": false
      },
      "routerModel": {
        "type": "string",
        "default": "github-copilot/gpt-5-mini",
        "description": "The lightweight model used for semantic skill dispatching."
      },
      "exemptions": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "default": ["zesty-*", "qmd"],
        "description": "List of skills to always include. Supports wildcards (e.g., 'zesty-*')."
      }
    }
  }
}
